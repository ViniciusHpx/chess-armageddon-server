{
  "private": true,
  "name": "chess-armageddon-server",
  "version": "0.1.0",
  "description": "Servidor autoritativo (Colyseus) da arena multiplayer de Chess Armageddon",
  "type": "module",
  "main": "build/index.js",
  "engines": {
    "node": ">= 20.9.0"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node build/index.js",
    "loadtest": "tsx loadtest/example.ts --room arena --numClients 8",
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "clean": "rimraf build",
    "typecheck": "tsc --noEmit",
    "test": "mocha -r tsx test/**/*.test.ts --exit --timeout 15000"
  },
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "@colyseus/loadtest": "^0.17.7",
    "@colyseus/testing": "^0.17.10",
    "@colyseus/sdk": "^0.17.17",
    "@types/express": "^4.17.1",
    "@types/mocha": "^10.0.1",
    "@types/node": "^22.13.13",
    "mocha": "^11.7.5",
    "rimraf": "^5.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@colyseus/tools": "^0.17.0",
    "@colyseus/schema": "^4.0.0",
    "colyseus": "^0.17.6",
    "express": "^4.18.2"
  }
}
